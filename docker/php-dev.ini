; Development PHP config - mounted into container to make code changes visible immediately
; Force OPcache to revalidate file timestamps on every request (development only)
opcache.enable=1
opcache.validate_timestamps=1
opcache.revalidate_freq=0
; Optional: reduce memory for dev
opcache.memory_consumption=64
; Keep other defaults
